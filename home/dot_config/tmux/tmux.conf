set -g prefix C-s

# evil but i like it 
set -g mouse on

# vim mode
setw -g mode-keys vi
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# easier copy mode
unbind [
unbind ]
bind -n M-c copy-mode # Alt+c to enter copy mode
bind -n M-v paste-buffer # Alt+v to paste

# status bar
set-option -g status-position top
